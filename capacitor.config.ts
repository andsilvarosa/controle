import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sos.controle',
  appName: 'SOS Controle Test',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
